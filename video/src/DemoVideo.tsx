import { AbsoluteFill } from "remotion";

// Placeholder — you will fill this in with real scenes
export const DemoVideo: React.FC = () => {
  return (
    <AbsoluteFill style={{ background: "#020617", color: "white", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <h1 style={{ fontSize: 64, fontWeight: 800 }}>Morpheus</h1>
    </AbsoluteFill>
  );
};
